# AI Agents Master — install with: pip install -r requirements.txt
# Then: python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.0.0
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0
python-dotenv>=1.0.0
openai>=1.0.0
